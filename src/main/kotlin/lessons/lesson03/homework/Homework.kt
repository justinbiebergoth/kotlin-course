package org.example.lessons.lesson03.homework

// 1. Название мероприятия (неизменяемая публичная переменная известная на момент написания)
val name: String = "Monster Hackaton"

// 2. Дата проведения (публичная переменная известная на момент написания, но может быть изменена из-за переноса)
var date: String = "10 ноября 2025"

// 3. Место проведения (публичная переменная известная на момент написания, но может быть изменена)
var location: String = "Москва, Крокус Экспо"

// 4. Подробный бюджет мероприятия, включая расходы на оборудование, кейтеринг и другие операционные расходы. (приватная неизменяемая переменная)
private val detailedBudget: String = "подробная информация с перечислением поставщиков и сумм для внутреннего пользования"

// 5. Количество участников (публичная изменяемая переменная с валидацией)
var participantCount: Int = 0
    set(value) {
        if (value >= 0) field = value
        else throw IllegalArgumentException("Количество участников не может быть отрицательным")
    }

// 6. Длительность хакатона (публичная неизменяемая переменная)
val duration: Int = 48 // часов

// 7. Контактная информация и условия соглашений с поставщиками пищи, оборудования и других услуг. (приватная неизменяемая переменная)
private val supplierAgreements: String = "конфиденциальная информация о поставщиках"

// 8. Текущее состояние хакатона (статус) (публичная изменяемая переменная)
var status: String = "планируется"

// 9. Список спонсоров (публичная изменяемая переменная)
var sponsors: String = ""

// 10. Бюджет мероприятия (публичная неизменяемая переменная)
val totalBudget: Int = 1000000

// 11. Текущий уровень доступа к интернету (приватная изменяемая переменная)
private var internetSpeed: Int = 100 // Мбит/с

// 12. Информация о транспортировке оборудования, распределении ресурсов и координации между различными командами поддержки. (приватная неизменяемая переменная)
private val logisticsInfo: String = "детали логистики для организаторов"

// 13. Количество команд (публичная изменяемая переменная)
var teamCount: Int = 0

// 14. Перечень задач (публичная изменяемая переменная)
var taskList: String = ""

// 15. План эвакуации (публичная неизменяемая переменная)
val evacuationPlan: String = "план эвакуации при чрезвычайных ситуациях"

// 16. Список доступного оборудования (публичная изменяемая переменная)
var availableEquipment: String = ""

// 17. Список свободного оборудования (публичная изменяемая переменная)
var freeEquipment: String = ""

// 18. График питания участников (публичная изменяемая переменная с отложенной инициализацией)
lateinit var mealSchedule: String

// 19. План мероприятий на случай сбоев (публичная неизменяемая переменная)
val contingencyPlan: String = "план действий при сбоях оборудования"

// 20. Список экспертов и жюри (публичная изменяемая переменная)
var judges: String = ""

// 21. Политика конфиденциальности (публичная неизменяемая переменная)
val privacyPolicy: String = "политика конфиденциальности мероприятия"

// 22. Приватные отзывы участников и зрителей (приватная изменяемая переменная)
private var feedback: String = ""

// 23. Текущая температура в помещении (приватная изменяемая переменная)
private var currentTemperature: Int = 22

// 24. Данные мониторинга сетевого оборудования (приватная изменяемая переменная)
private var networkMonitoringData: String = ""

// 25. Регулятор скорости интернета (приватная изменяемая переменная с валидацией)
private var internetSpeedRegulator: Double = 1.0
    set(value) {
        if (value in 0.1..1.0) field = value
        else throw IllegalArgumentException("Коэффициент должен быть от 0.1 до 1.0")
    }

// 26. Уровень освещения (приватная изменяемая переменная)
private var lightingLevel: Int = 100

// 27. Лог событий мероприятия (приватная изменяемая переменная)
private var eventLog: String = ""

// 28. Возможность получения медицинской помощи (публичная неизменяемая переменная)
val medicalAssistanceAvailable: Boolean = true

// 29. Планы безопасности мероприятия (публичная неизменяемая переменная)
val securityProcedures: String = "протоколы безопасности и чрезвычайных ситуаций"

// 30. Регистрационный номер мероприятия (публичная неизменяемая переменная)
val registrationNumber: String = "HACK-2025-001"

// 31. Максимально допустимый уровень шума (публичная неизменяемая переменная)
val maxNoiseLevel: Int = 70 // децибел

// 32. Индикатор превышения уровня шума (публичная изменяемая переменная)
var noiseLevelExceeded: Boolean = false

// 33. План взаимодействия с прессой (публичная изменяемая переменная)
var pressInteractionPlan: String = ""

// 34. Детальная публичная информация о проектах команд (публичная изменяемая переменная)
var teamProjectsInfo: String = ""

// 35. Статус получения разрешений (публичная изменяемая переменная)
var permitsStatus: String = "в процессе"

// 36. Индикатор доступности специализированного оборудования (публичная изменяемая переменная)
var specialEquipmentAvailable: Boolean = true

// 37. Список партнеров мероприятия (публичная изменяемая переменная)
var partners: String = ""

// 38. Отчет после завершения мероприятия (публичная переменная с отложенной инициализацией)
lateinit var postEventReport: String

// 39. Внутренние правила распределения призов (приватная неизменяемая переменная)
private val prizeDistributionRules: String = "правила распределения призов"

// 40. Список разыгрываемых призов (публичная изменяемая переменная)
var prizes: String = ""

// 41. Контактная информация экстренных служб (приватная неизменяемая переменная)
private val emergencyContacts: String = "контакты экстренных служб"

// 42. Особые условия для участников с ограниченными возможностями (публичная неизменяемая переменная)
val accessibilityFeatures: String = "условия для участников с ограниченными возможностями"

// 43. Общее настроение участников (публичная изменяемая переменная)
var participantMood: String = ""

// 44. Подробный план хакатона (публичная переменная с отложенной инициализацией)
lateinit var detailedSchedule: String

// 45. Имя знаменитого специального гостя (публичная переменная с отложенной инициализацией)
lateinit var specialGuest: String

// 46. Максимальное количество людей (публичная неизменяемая переменная)
val maxCapacity: Int = 500

// 47. Количество часов для работы над проектом (публичная неизменяемая переменная)
val workingHoursPerTeam: Int = 36

// 48. Ленивая инициализация для ресурсоемких вычислений (пример)
val resourceIntensiveData: String by lazy {
    // Выполняем сложные вычисления только при первом обращении
    performComplexCalculations()
}

private fun performComplexCalculations(): String {
    // Имитация сложных вычислений
    Thread.sleep(1000)
    return "результат сложных вычислений"
}