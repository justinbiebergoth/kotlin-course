// Задания для цикла for

// Прямой диапазон
// 1. Напишите цикл for, который выводит числа от 1 до 5.
println("Числа от 1 до 5:")
for (i in 1..5) {
    println(i)
}

// 2. Напишите цикл for, который выводит четные числа от 1 до 10.
println("\nЧетные числа от 1 до 10:")
for (i in 1..10) {
    if (i % 2 == 0) {
        println(i)
    }
}

// Обратный диапазон
// 3. Создайте цикл for, который выводит числа от 5 до 1.
println("\nЧисла от 5 до 1:")
for (i in 5 downTo 1) {
    println(i)
}

// 4. Создайте цикл for, который выводит числа от 10 до 1, уменьшая их на 2.
println("\nЧисла от 10 до 1 с шагом -2:")
for (i in 10 downTo 1 step 2) {
    println(i)
}

// С шагом (step)
// 5. Используйте цикл for с шагом 2 для вывода чисел от 1 до 9.
println("\nЧисла от 1 до 9 с шагом 2:")
for (i in 1..9 step 2) {
    println(i)
}

// 6. Напишите цикл for, который выводит каждое третье число в диапазоне от 1 до 20.
println("\nКаждое третье число от 1 до 20:")
for (i in 1..20 step 3) {
    println(i)
}

// Использование до (until)
// 7. Создайте числовую переменную 'size'. Используйте цикл for с шагом 2 для вывода чисел от 3 до size не включая size.
println("\nЧисла от 3 до size (не включая) с шагом 2:")
val size = 10
for (i in 3 until size step 2) {
    println(i)
}

// Задания для цикла while

// Цикл while
// 8. Создайте цикл while, который выводит квадраты чисел от 1 до 5.
println("\nКвадраты чисел от 1 до 5:")
var counter = 1
while (counter <= 5) {
    println("Квадрат $counter = ${counter * counter}")
    counter++
}

// 9. Напишите цикл while, который уменьшает число от 10 до 5. После этого вывести результат в консоль
println("\nУменьшение числа от 10 до 5:")
var number = 10
while (number >= 5) {
    println(number)
    number--
}
println("Финальный результат: $number")

// Цикл do while
// 10. Используйте цикл do while, чтобы вывести числа от 5 до 1.
println("\nЧисла от 5 до 1 (do while):")
var count = 5
do {
    println(count)
    count--
} while (count >= 1)

// 11. Создайте цикл do while, который повторяется, пока счетчик меньше 10, начиная с 5.
println("\nЦикл do while от 5 до 9:")
var counter2 = 5
do {
    println(counter2)
    counter2++
} while (counter2 < 10)

// Задания для прерывания и пропуска итерации

// Использование break
// 12. Напишите цикл for от 1 до 10 и используйте break, чтобы выйти из цикла при достижении 6.
println("\nЦикл с break при достижении 6:")
for (i in 1..10) {
    if (i == 6) {
        break
    }
    println(i)
}

// 13. Создайте цикл while, который бесконечно выводит числа, начиная с 1, но прерывается при достижении 10.
println("\nБесконечный цикл с break при 10:")
var infiniteCounter = 1
while (true) {
    println(infiniteCounter)
    if (infiniteCounter == 10) {
        break
    }
    infiniteCounter++
}

// Использование continue
// 14. В цикле for от 1 до 10 используйте continue, чтобы пропустить четные числа.
println("\nЦикл с continue для пропуска четных чисел:")
for (i in 1..10) {
    if (i % 2 == 0) {
        continue
    }
    println(i)
}

// 15. Напишите цикл while, который выводит числа от 1 до 10, но пропускает числа, кратные 3.
println("\nЦикл while с пропуском чисел, кратных 3:")
var counter3 = 0
while (counter3 < 10) {
    counter3++
    if (counter3 % 3 == 0) {
        continue
    }
    println(counter3)
}